use std::path::{Path, PathBuf};

/*
1. vérifier si il n'existe deja pas si non Créer un dossier.skt/
2. ajouté les fichiers nécessaires 
*/


pub const SKT_DIR = ".skt";

pub async fn run_command_init(path: PathBuf) -> skc_error::Result<()> {
    
    
    
    Ok(())
}


async fn setup_skite_workspace(base_path: &Path) -> skc_error::Result<()> {}
